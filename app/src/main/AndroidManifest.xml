<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.health.healthbloom">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Common.FinalResult"></activity>
        <activity android:name=".Common.Exercises" />
        <activity android:name=".Common.SymptomSearch" />
        <activity android:name=".User.Account.ChangePassSuccessPromt" />
        <activity android:name=".User.Account.CreateNewPass" />
        <activity android:name=".User.Account.ModeOfPassReset" />
        <activity android:name=".User.Account.ForgotPassword" />
        <activity android:name=".User.Account.OtpVerification" />
        <activity android:name=".User.Account.Signup3" />
        <activity android:name=".User.Account.Signup2" />
        <activity android:name=".User.Account.Signup1" />
        <activity android:name=".User.Account.LoginOrSignup" />
        <activity android:name=".Common.Dashboard" />
        <activity android:name=".User.Account.Login" />
        <activity android:name=".Common.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>